1.
  a. What is the difference between declaring and initializing a variable? declaring it gives it a value, initializing it make it exist.
  b. Can you declare a variable without initializing it? no
  c. Can you initialize a variable without declaring it? yes
  d. Can you print a variable without initializing it? no

2.
  a. List all 8 primitive data types.
int
double
boolean
short
byte
float
long
char
  b. Identify the primitive data types that store integers only. List them in increasing order of memory size.
byte
short
int
long
  c. Identify the primitive data types that store numbers including non-integers.
     Which one has more precision and takes up more memory space?
float
double
  d. What do the remaining primitive data types store?
They store true/false or a single character
  
3. Explain everything that is wrong with the following code:
int i;
System.out.println(i);
int i = 5;
j = 7;
System.out.println(k);
k is not initialized

4. Use the following variables to print "5false8"
int i = 5;
boolean j = false;
int k = 2;
long l = 6;
System.out.println(""+i+j+(k+l));

5. Determine the output:
int i = 1;
int j = 2;
int k = 3;
System.out.println(i + "abc" + j + k); //1abc23
System.out.println(i + "abc" + (j + k)); //1abc5

6. Determine the output:
int i = 9;
int j = 2;
i %= j;
System.out.println("" + i + j); //19

7. Determine the output:
  a. int q = 3;
     System.out.println(q++); //3
  b. int q = 3;
     System.out.println(++q); //5

8.
  a. Determine the output:
  double d1 = 5.7;
  double d2 = 9.5;
  d1 = d2;
  d2 = d1;
  System.out.println(d1 + " " + d2); //5.7 5.7
  
  b. Did the code successfully swap the variables? If not, fix the code to make it swap the variables.
  double d1 = 5.7;
  double d2 = 9.5;
  double d3 = d1;
  d1 = d2;
  d2 = d3;
  System.out.println(d1 + "" + c2); //9.5 5.7
